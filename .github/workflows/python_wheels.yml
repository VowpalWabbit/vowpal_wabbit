name: Python

# This file builds and tests Python binary wheels across major platforms. The
# wheels that are produced are used for PyPi releases. The test step uses a
# fresh job and installs the wheel the ensure it works in isolation. The is also
# a job which produces the source distribution and ensures that can be built to.

on:
  push:
    branches:
      - master
      - 'releases/**'
      - rlos2023/test
  pull_request:
    branches:
      - '*'
      - rlos2023/test
      
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.sha }}
  cancel-in-progress: true

jobs:
  linux-python-build:
    name: manylinux.amd64.py${{ matrix.config.version}}.build
    runs-on: ${{ matrix.config.os }}
    strategy:
      matrix:
        config:
        - { version: "3.10", os: "ubuntu-22.04", boost_suffix: "310" }
      fail-fast: false
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build libboost-dev libboost-python-dev python3-dev python3-pip zlib1g-dev
      - name: Diagnostic - Check Python and Boost configuration
        shell: bash
        run: |
          echo "=== Python Information ==="
          which python3
          python3 --version
          python3 -c "import sys; print(f'Python path: {sys.executable}')"
          python3 -c "import sysconfig; print(f'Include dir: {sysconfig.get_path(\"include\")}')"

          echo -e "\n=== Boost Python Libraries ==="
          dpkg -L libboost-python-dev | grep -E '\.so' || echo "No .so files found"
          find /usr/lib -name '*boost_python*' 2>/dev/null || echo "No boost_python libs in /usr/lib"

          echo -e "\n=== Boost Version ==="
          dpkg -l | grep libboost-python

          echo -e "\n=== CMake Python Detection Test ==="
          cat > /tmp/test_cmake.txt << 'TESTEOF'
          cmake_minimum_required(VERSION 3.10)
          project(test)
          find_package(Python3 COMPONENTS Interpreter Development)
          find_package(Boost COMPONENTS python3)
          message(STATUS "Python3_FOUND: ${Python3_FOUND}")
          message(STATUS "Python3_VERSION: ${Python3_VERSION}")
          message(STATUS "Python3_EXECUTABLE: ${Python3_EXECUTABLE}")
          message(STATUS "Python3_INCLUDE_DIRS: ${Python3_INCLUDE_DIRS}")
          message(STATUS "Boost_FOUND: ${Boost_FOUND}")
          message(STATUS "Boost_VERSION: ${Boost_VERSION}")
          TESTEOF
          cmake -P /tmp/test_cmake.txt 2>&1 || true
      - name: Build wheel
        shell: bash
        run: |
          python3 -m pip install wheel setuptools auditwheel
          CMAKE_ARGS="-DSTATIC_LINK_VW_JAVA=On -DCMAKE_BUILD_TYPE=Release -DBoost_NO_BOOST_CMAKE=ON" python3 -m pip wheel . -w wheel_output/ --verbose
          auditwheel repair wheel_output/*whl -w audit_output/
      - name: Upload built wheel
        uses: actions/upload-artifact@v4
        with:
          name: manylinux_amd64_${{ matrix.config.version }}
          path: audit_output/
  linux-python-test:
    name: manylinux.amd64.py${{ matrix.version }}.test
    needs: linux-python-build
    container:
      image: python:${{ matrix.version }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        version: ["3.10"]
      fail-fast: false
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - uses: actions/download-artifact@v4
        with:
          name: manylinux_amd64_${{ matrix.version }}
          path: built_wheel
      - name: Test wheel
        shell: bash
        run: |
          pip install -r requirements.txt
          pip install pytest vw-executor twine
          pip install --force-reinstall built_wheel/*.whl
          twine check built_wheel/*.whl
          python -m pytest ./python/tests/
          python ./python/tests/run_doctest.py
      - name: Run vw tests as Python module
        shell: bash
        # Onethread is not supported in the Python wrapper so those tests must be skipped
        # Stdin is not supported
        # Help output tests assume --onthread is in the output
        # Fail tests are not included because the python stack trace causes the output to differ
        # Daemon tests are skipped
        # Tests without datafiles are skipped
        run: |
          cd test
          python run_tests.py -E 0.001 -f --skip_spanning_tree_tests --vw_bin_path "python3 -m vowpalwabbit" --skip_test 60 61 92 96 149 152 177 193 194 195 220 275 276 324 325 326 349 350 356 357 358 385 389 390 391 392 393 400 399 401 403 431 440
  linux-python-sdist-bundle:
    name: ubuntu-latest.amd64.py3.10.sdist-bundle
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          architecture: 'x64'
      - name: Install dependencies
        shell: bash
        run: python setup.py sdist
      - name: Upload built wheel
        uses: actions/upload-artifact@v4
        with:
          name: python_source_distribution
          path: dist/*.tar.gz
          if-no-files-found: error
  linux-python-sdist-build-test:
    name: ubuntu-latest.amd64.py3.10.sdist-build-test
    needs: linux-python-sdist-bundle
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-python@v4
      - uses: actions/download-artifact@v4
        with:
          name: python_source_distribution
          path: dist/
      - name: Install dependencies
        shell: bash
        run: |
          sudo apt update
          sudo apt install libboost-test-dev libboost-python-dev zlib1g-dev cmake ninja-build g++
      - name: Install source dist
        shell: bash
        run: pip install dist/*.tar.gz
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - name: Install dependencies
        shell: bash
        run: |
          pip install -r requirements.txt
          pip install pytest vw-executor
      - name: Run unit tests
        shell: bash
        run: |
          python -m pytest ./python/tests/
  linux-python-build-aarch64:
    name: manylinux.aarch64.py${{ matrix.config.version }}.build
    # Aarch builds are slow and are only run on push and not PR
    if: ${{ github.event_name == 'push' }}
    strategy:
      matrix:
        config:
        - { version: "3.10", base_path: /opt/python/cp310-cp310/, include_dir_name: python3.10/ }
      fail-fast: false
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        submodules: recursive
    - name: Set up QEMU
      id: qemu
      uses: docker/setup-qemu-action@v1
    - name: Pull image
      run: docker pull vowpalwabbit/manylinux2014_aarch64-build
    - name: Build Wheel
      run: |
            docker run --rm -v ${{ github.workspace }}:/ws:rw --workdir=/ws \
            vowpalwabbit/manylinux2014_aarch64-build \
            bash -exc 'CMAKE_ARGS="-DSTATIC_LINK_VW_JAVA=On -DPython_INCLUDE_DIR='${{ matrix.config.base_path }}include/${{ matrix.config.include_dir_name }}'" ${{ matrix.config.base_path }}bin/pip wheel . -w wheel_output/ --verbose && \
            auditwheel repair wheel_output/*whl -w audit_output/'
    - name: Upload built wheel
      uses: actions/upload-artifact@v4
      with:
        name: manylinux_aarch64_${{ matrix.config.version }}
        path: audit_output/
  linux-python-test-aarch64:
    name: manylinux.aarch64.py${{ matrix.config.version }}.test
    needs: linux-python-build-aarch64
    # Aarch builds are slow and are only run on push and not PR
    if: ${{ github.event_name == 'push' }}
    strategy:
      matrix:
        config:
        - { version: "3.10" }
      fail-fast: false
    env:
      py: python${{ matrix.config.version }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        submodules: recursive
    - name: Set up QEMU
      id: qemu
      uses: docker/setup-qemu-action@v1
    - uses: actions/download-artifact@v4
      with:
        name: manylinux_aarch64_${{ matrix.config.version }}
        path: built_wheel
    - name: Test Wheel
      run: |
            docker run --rm -v ${{ github.workspace }}:/io:rw --workdir=/io \
            arm64v8/ubuntu \
            bash -exc 'apt-get update && \
            apt install software-properties-common -y && \
            add-apt-repository ppa:deadsnakes/ppa -y && \
            DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends tzdata && \
            apt install -y ${{ env.py }} && \
            apt install -y ${{ env.py }}-venv && \
            ${{ env.py }} -m venv .env && \
            source .env/bin/activate && \
            pip install --upgrade pip && \
            pip install -r requirements.txt && \
            pip install pytest vw-executor twine && \
            pip install --force-reinstall built_wheel/*.whl && \
            twine check built_wheel/*.whl && \
            python --version && \
            python -m pytest ./python/tests/ && \
            deactivate'
  macos-python-build:
    name: macos.amd64.py${{ matrix.config.version }}.build
    runs-on: macos-15-intel
    strategy:
      matrix:
        config:
        - { version: "3.10", include_dir_name: python3.10/}
      fail-fast: false
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          python-version: ${{ matrix.config.version }}
          miniconda-version: "latest"
      - name: Build wheel
        shell: bash -l {0}
        run: |
          conda info
          conda install python=${{ matrix.config.version }} wheel zlib boost py-boost flatbuffers
          CMAKE_ARGS="-DSTATIC_LINK_VW_JAVA=On -DPython_INCLUDE_DIR=\"$CONDA_PREFIX/include/${{ matrix.config.include_dir_name }}\"" pip wheel . -w wheel_output/ --verbose
      - name: Upload built wheel
        uses: actions/upload-artifact@v4
        with:
          name: macos_amd64_${{ matrix.config.version }}
          path: wheel_output/
  macos-python-test:
    name: macos.amd64.py${{ matrix.version }}.test
    needs: macos-python-build
    runs-on: macos-15-intel
    strategy:
      matrix:
        version: ["3.10"]
      fail-fast: false
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          python-version: ${{ matrix.version }}
          miniconda-version: "latest"
      - uses: actions/download-artifact@v4
        with:
          name: macos_amd64_${{ matrix.version }}
          path: built_wheel
      - name: Test wheel
        shell: bash -l {0}
        run: |
          conda install python=${{ matrix.version }} boost py-boost
          pip install -r requirements.txt
          pip install pytest vw-executor twine
          pip install --force-reinstall built_wheel/*.whl
          twine check built_wheel/*.whl
          python -m pytest ./python/tests/
          python ./python/tests/run_doctest.py
  windows-python-build:
    name: windows-2019.amd64.py${{ matrix.config.version }}.build
    runs-on: windows-2019
    env:
      VCPKG_ROOT: ${{ github.workspace }}\\vcpkg
      VCPKG_REF: 25b458671af03578e6a34edd8f0d1ac85e084df4
      VCPKG_DEFAULT_BINARY_CACHE: ${{ github.workspace }}\vcpkg_binary_cache
    strategy:
      matrix:
        config:
        - { version: "3.10", vcpkg_manifest: "python\\vcpkg_python310.json", overlay_ports: "" }
      fail-fast: false
    steps:
      - uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.config.version }}
      - uses: actions/checkout@v3
        with:
          submodules: recursive
          path: ${{github.workspace}}\vowpal_wabbit
      - uses: actions/checkout@v3
        with:
          path: ${{ env.VCPKG_ROOT }}
          repository: 'microsoft/vcpkg'
          ref: ${{ env.VCPKG_REF }}
          # We need a deep clone so that we can go back in history for version selection.
          fetch-depth: '0'
      - name: Restore vcpkg and build artifacts
        uses: actions/cache@v3
        with:
          path: |
            ${{ env.VCPKG_DEFAULT_BINARY_CACHE }}
          key: |
            ${{ env.VCPKG_REF }}-${{ matrix.config.version }}-python-win-cache-invalidate-6
      - run: ${{ env.VCPKG_ROOT }}/bootstrap-vcpkg.bat
      - name: Build wheel
        shell: powershell
        run: |
          if (![System.IO.Directory]::Exists("${{ env.VCPKG_DEFAULT_BINARY_CACHE }}"))
          {
              New-Item -ItemType Directory -Force -Path "${{ env.VCPKG_DEFAULT_BINARY_CACHE }}"
          }
          if (![System.IO.Directory]::Exists("${{github.workspace}}\\wheel_output"))
          {
              New-Item -ItemType Directory -Force -Path "${{github.workspace}}\\wheel_output"
          }
          ls ${{ github.workspace }}
          ls ${{ env.VCPKG_DEFAULT_BINARY_CACHE }}
          echo "=== Manually install vcpkg dependencies first ==="
          cd ${{ env.VCPKG_ROOT }}
          .\vcpkg.exe install --triplet x64-windows zlib boost-python python3 flatbuffers

          echo "=== List installed packages ==="
          .\vcpkg.exe list

          echo "`n=== Checking installed Boost.Python libraries ==="
          echo "Boost libraries in lib directory:"
          Get-ChildItem "${{ env.VCPKG_ROOT }}\installed\x64-windows\lib" -Filter "*boost*python*.lib" -ErrorAction SilentlyContinue | Select-Object Name

          echo "`nBoost.Python CMake modules:"
          Get-ChildItem "${{ env.VCPKG_ROOT }}\installed\x64-windows\share\boost" -Recurse -Filter "*python*.cmake" -ErrorAction SilentlyContinue | Select-Object Name

          echo "=== Verify vcpkg installation directories ==="
          echo "Checking: ${{ env.VCPKG_ROOT }}\\installed"
          if (Test-Path "${{ env.VCPKG_ROOT }}\\installed") {
              echo "installed/ directory exists"
              Get-ChildItem "${{ env.VCPKG_ROOT }}\\installed" | Select-Object Name
          } else {
              echo "WARNING: installed/ directory does NOT exist"
          }

          echo "`nChecking: ${{ env.VCPKG_ROOT }}\\installed\\x64-windows"
          if (Test-Path "${{ env.VCPKG_ROOT }}\\installed\\x64-windows") {
              echo "x64-windows/ directory exists"
              Get-ChildItem "${{ env.VCPKG_ROOT }}\\installed\\x64-windows" | Select-Object Name
          } else {
              echo "WARNING: x64-windows/ directory does NOT exist"
          }

          echo "`nLooking for zlib files:"
          Get-ChildItem -Path "${{ env.VCPKG_ROOT }}\\installed" -Recurse -Filter "*zlib*" -ErrorAction SilentlyContinue | Select-Object FullName | Format-Table -AutoSize

          echo "`nChecking for vcpkg_installed/ (manifest mode):"
          if (Test-Path "${{github.workspace}}\\vowpal_wabbit\\vcpkg_installed") {
              echo "WARNING: vcpkg_installed/ exists - manifest mode may be active"
              Get-ChildItem "${{github.workspace}}\\vowpal_wabbit\\vcpkg_installed" -Recurse -Depth 2 | Select-Object FullName
          } else {
              echo "OK: vcpkg_installed/ does not exist (not in manifest mode)"
          }

          cd ${{github.workspace}}\\vowpal_wabbit
          pip install wheel setuptools
          $env:CMAKE_GENERATOR = "Visual Studio 16 2019"

          echo "=== Building wheel with setup.py vcpkg support ==="
          echo "CMAKE_GENERATOR: $env:CMAKE_GENERATOR"
          echo "VCPKG_ROOT: ${{ env.VCPKG_ROOT }}"

          $vcpkgRoot = "${{ env.VCPKG_ROOT }}" -replace '\\+', '/'
          $vcpkgInstalled = "$vcpkgRoot/installed/x64-windows"
          $boostRoot = $vcpkgInstalled
          $boostIncludeDir = "$vcpkgInstalled/include"

          echo "VCPKG_ROOT (normalized): $vcpkgRoot"
          echo "vcpkg installed dir: $vcpkgInstalled"
          echo "BOOST_ROOT: $boostRoot"
          echo "Boost_INCLUDE_DIR: $boostIncludeDir"

          echo "`n=== DETAILED BOOST DIAGNOSTICS ==="
          echo "All boost library files:"
          Get-ChildItem "${{ env.VCPKG_ROOT }}\installed\x64-windows\lib" -Filter "boost_*.lib" | ForEach-Object { $_.Name }

          echo "`nAll boost DLL files:"
          Get-ChildItem "${{ env.VCPKG_ROOT }}\installed\x64-windows\bin" -Filter "boost_*.dll" -ErrorAction SilentlyContinue | ForEach-Object { $_.Name }

          echo "`nBoost cmake config directories:"
          Get-ChildItem "${{ env.VCPKG_ROOT }}\installed\x64-windows\share" -Directory -Filter "boost*" | ForEach-Object { $_.Name }

          echo "`nContents of share/boost-python (if exists):"
          if (Test-Path "${{ env.VCPKG_ROOT }}\installed\x64-windows\share\boost-python") {
            Get-ChildItem "${{ env.VCPKG_ROOT }}\installed\x64-windows\share\boost-python" | ForEach-Object { $_.Name }
          } else {
            echo "  (directory doesn't exist)"
          }

          echo "`n=== END DIAGNOSTICS ===`n"

          python setup.py `
            --vcpkg-root="${{ env.VCPKG_ROOT }}" `
            --cmake-generator="Visual Studio 16 2019" `
            --cmake-options="-DSTATIC_LINK_VW_JAVA=On;-DCMAKE_BUILD_TYPE=Release;-DBOOST_ROOT=$boostRoot;-DBoost_INCLUDE_DIR=$boostIncludeDir;-DBoost_NO_SYSTEM_PATHS=ON;-DBOOST_PY_VERSION_SUFFIX=310" `
            bdist_wheel --dist-dir="${{github.workspace}}\\wheel_output\\"
      - name: Upload built wheel
        uses: actions/upload-artifact@v4
        with:
          name: windows_amd64_${{ matrix.config.version }}
          path: wheel_output
  windows-python-test:
    name: ${{ matrix.os }}.amd64.py${{ matrix.version }}.test
    needs: windows-python-build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        version: ["3.10"]
        os: ["windows-2019"]
      fail-fast: false
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.version }}
      - uses: actions/download-artifact@v4
        with:
          name: windows_amd64_${{ matrix.version }}
          path: built_wheel
      - name: Install deps and test wheel
        shell: bash
        run: |
          export wheel_files=(built_wheel/*)
          export wheel_file="${wheel_files[0]}"
          echo Installing ${wheel_file}...
          pip install -r requirements.txt
          pip install pytest vw-executor twine
          pip install --force-reinstall ${wheel_file}
          twine check ${wheel_file}
          python -m pytest .\\python\\tests\\
          python .\\python\\tests\\run_doctest.py
