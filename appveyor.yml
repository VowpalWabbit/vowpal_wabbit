version: 1.0.{build}
configuration:
- Release
platform:
- Win32
- x64
build:
  verbosity: minimal
test_script:
- ps: >-
    if($env:PLATFORM -eq "x64")
    {
        vstest.console /Platform:x64 /inIsolation bin\x64\Release\explore_tests.dll
    }