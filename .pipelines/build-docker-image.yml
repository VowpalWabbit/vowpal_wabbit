pool:
  vmImage: 'ubuntu-16.04'

steps:
  - script: docker build -t $(dockerOrgId)/$(imageName) .
  - script: |
    docker login -u $(dockerCredId) -p $(pswd) https://index.docker.io/v1/
    docker push $(dockerOrgId)/$(imageName)