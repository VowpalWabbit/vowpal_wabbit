* speed test runs only if invoked with SPEEDTEST=y
  * .NOTPARALLEL for speed tests
  * if running speed test, then wrap in `time`, and dump its output to speed result file

* VPATH ?

* make vw stop if data file is missing
