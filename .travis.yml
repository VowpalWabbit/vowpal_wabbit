language: cpp
compiler:
  - clang
  - gcc
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq libboost-all-dev
matrix:
    include:
        - install: make
        - install: ./autogen.sh && ./configure && make
script: make test
